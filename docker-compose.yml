services:
  cloudflare-ip-updater:
    build: .
    container_name: cloudflare-ip-updater
    restart: unless-stopped
    environment:
      - CLOUDFLARE_ZONE_ID=
      - CLOUDFLARE_API_TOKEN=
      - DOMAIN=
      - DISCORD_WEBHOOK_URL=
